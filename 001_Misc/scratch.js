function stockPrices(input) {
    let mostRecent = {};
    const prices = {};
    const dates = [];
    for (let i = 0; i < input.length; i++) {
      let company = input[i]
      prices[i] = {}
      for (let j = 0; j < company.length; j++) {
        dates.push[company[j][0]]
        prices[i] 
      }
    }
}

stockPrices({});
